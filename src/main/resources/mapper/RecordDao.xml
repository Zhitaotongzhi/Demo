<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.RecordDao">
    <!--查询所有咨询记录-->
    <select id="findAllRecord" resultType="com.example.demo.domain.Record">
        select * from record limit #{pageNum}, #{pageSize}
    </select>
    <select id="findAllRecordCount" resultType="long">
        select count(1) from record
    </select>
    <!--查找对应访客的咨询记录-->
    <select id="findAllRecordByName" resultType="com.example.demo.domain.Record">
        select * from record where v_name like #{visitorName}"%" limit #{pageNum}, #{pageSize}
    </select>
    <select id="findAllRecordByNameCount" resultType="long">
        select count(1) from record where v_name = #{visitorName}
    </select>
    <!--查找对应咨询师的咨询记录-->
    <select id="findAllRecordByConName" resultType="com.example.demo.domain.Record">
        select * from record where c_name like #{counselorName}"%" limit #{pageNum}, #{pageSize}
    </select>
    <select id="findAllRecordByConNameCount" resultType="long">
        select count(1) from record where c_name = #{counselorName}
    </select>
    <!--查找对应咨询师和访客的咨询记录-->
    <select id="findAllRecordByNameAndUserame" resultType="com.example.demo.domain.Record">
        select * from record where v_name = #{visitorName} and c_name = #{counselorName} limit #{pageNum}, #{pageSize}
    </select>
    <!--查找对应时间的咨询记录-->
    <select id="findAllRecordByTime" resultType="com.example.demo.domain.Record">
        select * from record where starttime between #{startDate} and #{endDate}
    </select>
    <!--访客一段时间内的咨询记录-->
    <select id="findAllRecordByNameAndTime" resultType="com.example.demo.domain.Record">
        select * from record where v_name = #{visitorName} and starttime between #{startDate} and #{endDate}
    </select>

    <!--咨询师一段时间内的咨询记录-->
    <select id="findAllRecordByUsernameAndTime" resultType="com.example.demo.domain.Record">
        select * from record where c_name = #{counselorName} and starttime between #{startDate} and #{endDate}
    </select>
    <!--查找满足所有条件的咨询记录-->
    <select id="findAllRecordByAll" resultType="com.example.demo.domain.Record">
        select * from record where v_name = #{visitorName} and c_name = #{counselorName} and starttime between #{startDate} and #{endDate}
    </select>
    <!--找到所有与该督导绑定的咨询师的name-->
    <select id="findAllBindByUsername" parameterType="String" resultType="String">
        select name from worker where bind_username = #{username}
    </select>
    <!--找到与自己绑定的咨询师的咨询记录-->
    <select id="findAllRecordBindByUsername" resultType="com.example.demo.domain.Record">
        select * from record <where>
        <if test="list != null and list.size() > 0">
            AND c_name in
                        <foreach collection="list" item="counselorName" open = "(" separator="," close=")">
                            #{counselorName}
                        </foreach>
        </if>
    </where> limit #{pageNum}, #{pageSize}
    </select>

    <select id="findAllBindRecordCount" resultType="long">
        select count(1) from record <where>
        <if test="list != null and list.size() > 0">
            AND c_name in
                        <foreach collection="list" item="counselorName" open="(" separator="," close=")">
                            #{counselorName}
                        </foreach>
        </if>
    </where>
    </select>
    <!--通过姓名查询与自己绑定的咨询师的咨询记录-->
    <select id="findAllBindRecordByName"  resultType="com.example.demo.domain.Record">
        select * from record <where>  <if test="list != null and list.size() > 0">
        AND c_name in <foreach collection="list" item="counselorName" open="(" separator="," close=")">
        #{counselorName}
    </foreach>
    </if> and v_name = #{name}
    </where>
    </select>
    <!--通过时间查询与自己绑定的咨询师的咨询记录-->
    <select id="findAllBindRecordByTime" resultType="com.example.demo.domain.Record">
        select * from record <where>
        <if test="list != null and list.size() > 0">
            AND c_name in <foreach collection="list" item="counselorName" open="(" separator="," close=")">
            #{counselorName}
        </foreach>
        </if> and starttime between #{startTime} and #{endTime}
    </where>
    </select>
    <!--返回访客最后一次咨询的时间-->
    <select id="findLatestTime" parameterType="String">
        select max(starttime) from record name = #{name}
    </select>
    <select id="findEvaluate" parameterType="String">
        select evaluate from record where starttime = #{time}
    </select>

    <!--咨询师查看的咨询记录-->
    <!--查看所有的咨询记录-->
    <select id="findAllSelfRecord" resultType="com.example.demo.domain.Record">
        select * from record where c_name = #{counselorName} limit #{pageNum}, #{pageSize}
    </select>
    <!--访客姓名查询-->
    <select id="findAllSelfRecordByName" resultType="com.example.demo.domain.Record">
        select * from record where c_name = #{counselorName} and v_name = #{visitorName} limit #{pageNum}, #{pageSize}
    </select>
    <!--时间查询-->
    <select id="findAllSelfRecordByTime" resultType="com.example.demo.domain.Record">
        select * from record where c_name = #{counselorName} and starttime between #{startDate} and #{endDate} limit #{pageNum}, #{pageSize}
    </select>
    <!--所有条件查询-->
    <select id="findAllSelfRecordByAll" resultType="com.example.demo.domain.Record">
        select * from record where c_name = #{counselorName} and v_name = {visitorName} and starttime between #{startDate} and #{endDate} limit #{pageNum}, #{pageSize}
    </select>

    <!--访客查看自己的咨询记录-->
    <select id="findAllVisitorRecordByPno" resultType="com.example.demo.domain.Record">
        select * from record where pno = #{pno}
    </select>

    <!--插入咨询记录-->
    <insert id="insertRecord" parameterType="com.example.demo.domain.Record">
        insert into record(c_name, v_name, pno, duration, starttime, level, evaluate, c_username) values (#{c_name}, #{v_name}, #{pno}, #{duration}, #{starttime}, #{level}, #{evaluate}, #{c_username})
    </insert>

    <!--统计当天咨询的总数-->
    <select id="totalCount" parameterType="String" resultType="int">
        select count(*) from record where starttime > #{currentDate}
    </select>
    <!--统计咨询师当天咨询的总数-->
    <select id="counselorRecordCount" parameterType="String" resultType="int">
        select count(*) from record where c_username = #{counselorUsername} and starttime > #{currentDate}
    </select>
    <!--统计今天的咨询总时长-->
    <select id="totalTimeCount" parameterType="String" resultType="java.sql.Time">
        select SEC_TO_TIME(sum(TIME_TO_SEC(duration))) from record where starttime > #{currentDate}
    </select>
    <select id="counselotTotalTime" resultType="java.sql.Time">
        select SEC_TO_TIME(sum(TIME_TO_SEC(duration))) from record where  c_username = #{counselorUsername} and starttime> #{currentDate}
    </select>
    <!--最近的咨询记录-->
    <select id="currentRecord" resultType="com.example.demo.domain.Record">
        select * from record ORDER BY starttime DESC limit #{limit}
    </select>
    <!--正在进行的咨询数量-->
    <select id="currentCount" resultType="long">
        select sum(current) from worker where authority = "Counselor"
    </select>
    <!--一位咨询师最近的咨询记录-->
    <select id="findAllNewRecord" resultType="com.example.demo.domain.Record">
        select * from record where c_username = #{counselorUsername} ORDER BY startttime DESC limit #{limit}
    </select>
    <!--统计某一位咨询师和某一位访客咨询记录的数量-->
    <!--<select id="v2cCount" resultType="int" parameterType="String">
        select count(1) from record where v_name = #{visitorName} and c_name = #{counselorName}
    </select>-->
</mapper>