<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.SupervisorDao">
    <select id="findAllSupervisorByAuthority" resultType="com.example.demo.domain.Supervisor">
        select * from worker where name=#{name} and authority = "Supervisor" limit #{pageNum},#{pageSize}
    </select>

    <select id="findAllSupervisorCount" resultType="long">
        select count(1) from worker where name = #{search} and authority = "Supervisor"
    </select>

    <select id="findAllSupervisor" resultType="com.example.demo.domain.Supervisor">
        select * from worker where authority = "Supervisor" limit #{pageNum}, #{pageSize}
    </select>

    <select id="findAllSupervisorCountNoName" resultType="long">
        select count(1) from worker where authority = "Supervisor"
    </select>

    <update id="bannedSupervisor">
        update worker set status = "banned" where username = #{username}
    </update>

    <update id="enableSupervisor">
        update worker set status = "normal" where username = #{username}
    </update>

    <select id="selectSupervisorByUsername" resultType="com.example.demo.domain.Supervisor">
        select * from worker where username = #{username}
    </select>

    <update id="updeteSupervisorByUsername">
        update worker <set>name = #{name}, gender = #{gender}, password = #{password}, title = #{title}, workunit = #{workunit}, pno = #{pno}, email = #{email} </set> where username = #{username}
    </update>

    <!--<select id="findAllSupervisor" resultType="com.example.demo.domain.Supervisor">
        select * from worker where authority = "Supervisor"
    </select>-->
    <!--删除所有督导-->
    <delete id="deleteSupervisor">
        delete from worker where username = #{username}
    </delete>
    <!--正在进行的求助数-->
    <select id="assistCount" resultType="int">
        select sum(current) from worker where authority = "Supervisor"
    </select>
    <!--找到在线的咨询师列表-->
    <select id="findAllSupIsonline" resultType="com.example.demo.domain.Supervisor">
        select * from worker where authority = "Supervisor" and is_online = "1"
    </select>
    <select id="onLineCount" resultType="long">
        select count(*) from worker where is_online = "1" and authority = "Supervisor" and status = "normal"
    </select>

    <!--更新正在进行的求助数量-->
    <update id="addCurrentAssist">
        update worker set current = current + 1 where username = #{username}
    </update>
    <update id="subCurrentAssist">
        update worker set current = current - 1 where username = #{username}
    </update>
    <!--督导总求助数-->
    <select id="totalCount" resultType="int">
        select totalOrder from worker where username = #{username}
    </select>

    <!--咨询师求助督导时看到的督导列表-->
    <!--<select id="BindSupIsonline" resultType="com.example.demo.domain.Supervisor">
        select * from worker where username = #{username} and is_online = "1"
    </select>
    <select id="bindUsername2Username" resultType="String ">
        select bind_username from worker where username = #{username}
    </select>-->

    <!--与某督导绑定的在线的咨询师-->
    <select id="findBindCounselorIsonline" resultType="com.example.demo.domain.Consultant">
        select * from worker where bind_username = #{username} and is_online = "1" limit #{pageNum}, #{pageSize}
    </select>

    <!--更新今日求助数和总求助数-->
    <update id="updateOrderAndTotalOrder">
        update worker set orderNumber = orderNumber + 1, totalOrder = totalOrder + 1 where username = #{username}
    </update>
</mapper>